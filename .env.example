# Optional: Override the default local Docker MongoDB with a remote cloud database (like MongoDB Atlas)
# Leave this blank or commented out to use the built-in dockerized database
# MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?appName=<AppName>
